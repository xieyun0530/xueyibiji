server:
  port: 8888

spring:
  # Redis相关配置
  redis:
    host: localhost
    database: 0
    port: 6379
#    password: 123456
    jedis:
      pool:
        max-active: 8
        max-wait: -1ms
        max-idle: 8
        min-idle: 0
    timeout: 5000ms